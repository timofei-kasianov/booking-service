spring:
  application:
    name: accommodation-service
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  application:
    name: accommodation-service
  profiles: docker
  cloud:
    config:
      uri: http://booking-config-server:8888
