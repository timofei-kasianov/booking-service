spring:
  application:
    name: service-registry
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  application:
    name: service-registry
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
