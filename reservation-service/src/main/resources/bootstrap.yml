spring:
  application:
    name: reservation-service
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  application:
    name: reservation-service
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
